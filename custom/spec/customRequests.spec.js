const angular = require('angular');
require('angular-mocks');

const customRequests = require('../common/js/customRequests.js');

describe('customRequestIllComponentController', () => {
  beforeEach( () => {});

  it('should be true', () => {
    expect(true).toBe(true);
  });
});